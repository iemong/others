# アルゴリズムとデータ構造

# アルゴリズムとは
- アルゴリズムとは「喪院代を解くための方法、手順」
- 二分探索法 : 真ん中で割ってどちらかに絞っていく方法 (年齢当てゲームを例に)
- 深さ優先探索 : 無数に考えられる選択肢に対して、とりあえず決め打ちして進み、ミスったら、次の選択肢を試す手法 (虫食算)
- 幅優先探索 : 出発点に近いところから順に探索する手法 (迷路)

# 計算量とオーダー記法
- 計算量 : computatinal complexity。 実装せずに、大まかに測ることのできる「ものさし」としての役割を持つ。 O記法を使う。
- 指数時間 : O(N!), O(2^N)
- 多項式時間 : O(NlogN), O)(N√N)
- 定数時間 : O(1)
- Ω記法とθ記法もある

# 全探索
## 線形探索法
一つ一つ順番に調べてく方法。計算量はO(N)
使い途: 条件を満たす場所(index)を知る。最小値を求める。ペアを探す。部分和。
